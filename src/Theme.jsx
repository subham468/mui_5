import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "skyblue",
    },
    secondary: {
      main: "#A020F0",
    },
    otherColor: {
      main: "#999",
    },
    magentaColor: "#CA1F7B",
  },
});
